You are analyzing a single frame extracted from a video in a decentralized science research DAO dataroom (project updates, lab demos, presentations, or outreach).

This is ONE frame from a longer video — describe only what is visible in this frame. Transcribe on-screen text, slide titles, and labels if readable.

Respond with ONLY a JSON object (no markdown fences):

{"description": "<what this frame shows>", "transcribed_text": "<visible text or empty string>", "labels": ["<short tags>"]}
